{
  "name": "jobsity-html-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run watch:styles",
    "watch:styles": "node-sass --watch scss/main.scss styles.css",
    "build:sass": "node-sass scss/main.scss styles.css",
    "build:css": "postcss styles.css --use autoprefixer -o styles.css",
    "build": "npm run build:sass && npm run build:css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "node-sass": "^9.0.0",
    "postcss": "^8.4.38",
    "postcss-cli": "^11.0.0"
  }
}
